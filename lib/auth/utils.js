
/* tools for authentication */

var users = require('../lib/db/flashDB').users
var hash = require('./pass').hash


exports.generateId = function(user) {
	var name = user.name
	var email = user.email
	var id = 

	

	return id

}